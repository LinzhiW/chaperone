# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
build/
# electron-builder output
release/

# Vite cache
.vite/

# Per-install runtime data written next to the backend's .env
usage.json
rates.json
**/usage.json
**/rates.json

# Environment & secrets
.env
.env.local
.env.*.local
**/.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Superpowers cache (if local)
.superpowers/

# Private working material — kept locally, never published.
# These stay out of the repo even as they keep being updated.
# (docs/SESSION_HANDOFF.md has long flagged the content assets as "do NOT commit";
#  this makes that intent enforced rather than remembered.)
.design/agent-company/chats/
docs/product-intro.md
docs/weekly-report.md
docs/weekly/
regression_*.png
docs/screenshots/regression_*

# Design-process history: superseded plans and snapshot copies of the wireframes.
# Kept locally; they are a record of how the design got here, not something a
# reader of this repo needs. (phase2b-13a is still referenced from code.)
.design/**/DESIGN_LOG.md
.design/agent-company/project/backups/phase1/
.design/agent-company/project/backups/phase2a/
.design/agent-company/project/backups/phase2b/
.design/agent-company/project/uploads/
docs/DEV_PLAN.md
docs/DESIGN_NOTES.md
.design/agent-company/project/CLAUDE_CODE_KICKOFF.md
